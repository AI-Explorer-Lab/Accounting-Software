# Accounting Software Frontend

Vue 3 + Vite + TypeScript architecture scaffold.

```bash
npm install
npm run dev
```

The development server runs at `http://127.0.0.1:5173` and proxies `/api`
requests to the FastAPI backend at `http://127.0.0.1:8000`.
