default:
  environment:
    name: development
    debug: false
  server:
    host: 127.0.0.1
    port: 8100
    cors_origins:
      - http://127.0.0.1:8100
      - http://localhost:8100
  agent:
    repo_root: null
    validation_timeout_seconds: 900
    max_parallel_projects: 2
    harness_enabled: true
    knowledge:
      repo_root: /Users/mon/Documents/Knowledge-Base
      mcp_registry: /Users/mon/Documents/mcp/registry.json
      knowledge_writer_actor_id: orchestrator
    projects:
      - id: accounting
        name: Accounting Software
        repo_root: .
        default: true
        knowledge_actor_id: zhangsan
  notifications:
    webhook_url: null
    smtp_host: null
    smtp_port: 587
    smtp_starttls: true
    smtp_username: null
    smtp_password: null
    smtp_sender: null
    smtp_recipient: null

development:
  environment:
    name: development
    debug: true

test:
  environment:
    name: test
    debug: false

production:
  environment:
    name: production
    debug: false
